
describe('checkLoginTokenKey', () => 
{
  it.todo('Ensures PublicKey structure.');
});


describe('getLoginTokenKey', () => 
{
  it.todo('Fetches key from API and stores it in cache.');

  it.todo('Returns key from cache if not expired.');

  it.todo('Fetches key from API and stores it in cache if cached key expired.');
});
